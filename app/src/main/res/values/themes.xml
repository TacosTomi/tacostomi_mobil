<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Restaurante" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/periwinkle</item>
        <item name="colorOnPrimary">@color/blanco</item>
        <item name="colorPrimaryContainer">@color/periwinkle_claro</item>
        <item name="colorOnPrimaryContainer">@color/navy</item>
        <item name="colorSecondary">@color/navy</item>
        <item name="android:statusBarColor">@color/navy</item>
        <item name="android:windowBackground">@color/fondo</item>
    </style>

    <!-- La marquita de dónde estás, chiavo -->
    <style name="BottomNavIndicador" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/periwinkle</item>
    </style>
</resources>
